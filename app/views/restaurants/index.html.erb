<h1>hola</h1>

<div class="container mt-4">
  <h1>My restaurants</h1>

    <% @restaurants.each do |restaurants|%>
  <div class="border mt-2 mb-2 p-0 w-25 shadow bg-body-tertiary rounded">
    <p class="mt-4 mb-0 text-center "> <%= link_to restaurants.name, restaurant_path(restaurants), class:"text-decoration-none text-primary fs-2" %> </p>
    </div>
      <% end %>

  <p class="mt-3"> <%= link_to "create new", new_restaurant_path, class:"text-decoration-none btn btn-primary btn-sm" %> </p>
</div>
